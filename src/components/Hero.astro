<section
  class="w-full flex flex-col gap-5 justify-center items-center px-4 py-32"
>
  <div class="flex flex-col gap-2">
    <h1
      class="w-full max-w-xl sm:text-center text-3xl sm:text-5xl md:text-7xl font-medium tracking-tighter text-pretty"
    >
      AAC For All<br />
      <span class="text-2xl sm:text-4xl block mt-2 font-normal text-primary">
        Low-cost Android AAC tablets & free open-source apps
      </span>
    </h1>
    <p
      class="w-full max-w-xl text-muted-foreground sm:text-center text-normal sm:text-lg md:text-xl font-light tracking-tighter text-balance"
    >
      For non-speaking/autistic children. No tech barrier. No markup.<br/>
      Communication should never be limited by price tags or closed systems.
    </p>
  </div>
  <div class="w-full flex flex-wrap gap-3 items-center justify-center">
    <a
      href="/apk/"
      class="relative w-fit flex justify-center items-center px-4 py-2 sm:px-6 sm:py-3 sm:text-lg font-medium text-background bg-primary rounded-full perfect-shadow"
    >
      📥 Free AAC Apps
      <div
        class="z-[-1] bg-gradient-to-b from-secondary via-primary to-primary absolute w-[calc(100%-4px)] h-[calc(100%-4px)] rounded-full border-t-2 border-secondary"
      >
      </div>
    </a>
    <a
      href="/assets/AAC_App_Installation_Guide.pdf"
      target="_blank"
      rel="noopener noreferrer"
      class="relative w-fit flex justify-center items-center px-4 py-2 sm:px-6 sm:py-3 sm:text-lg font-medium text-background bg-primary rounded-full perfect-shadow"
    >
      📖 AAC Setup Guide
      <div
        class="z-[-1] bg-gradient-to-b from-secondary via-primary to-primary absolute w-[calc(100%-4px)] h-[calc(100%-4px)] rounded-full border-t-2 border-secondary"
      >
      </div>
    </a>
  </div>
  <img src="/assets/hero-bg.jpg" alt="AAC for All hero banner" class="w-full max-w-2xl rounded-2xl shadow-lg mt-10" />
</section>

<style>
  @keyframes heroIn {
    0% {
      opacity: 0;
      transform: translateY(0.5rem);
    }
    60% {
      opacity: 1;
      transform: translateY(-0.05rem);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }

  h1 {
    opacity: 0;
    animation: heroIn 0.7s cubic-bezier(0.5, 1.15, 0.2, 1.1) forwards;
  }
  p {
    opacity: 0;
    animation: heroIn 0.7s cubic-bezier(0.5, 1.15, 0.2, 1.1) 0.3s forwards;
  }
  a {
    opacity: 0;
    animation: heroIn 0.7s cubic-bezier(0.5, 1.15, 0.2, 1.1) 0.6s forwards;
  }
</style>
